import { render } from '@testing-library/react';
import { Timeline } from './Timeline';

describe('<Sidebar />', () => {
    it('', () => {
        render(<Timeline date="" text="teste..." isLastLine />);
    });
});
