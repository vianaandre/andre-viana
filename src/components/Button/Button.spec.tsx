import { render } from '@testing-library/react';
import { Button } from './Button';

describe('<Sidebar />', () => {
  it('', () => {
    render(<Button typeStyle="outline" title="Teste" />);
  });
});
